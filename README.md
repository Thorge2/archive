# Archive

> Archive is a Web app for publishing articles.

## Running it

To run this app the easiest method is to use `docker` and `docker-compose`.
Just execute

```bash
docker-compose up
```

and everything will be done fpr you.
You can now view the app in your browser under `http://localhost`
